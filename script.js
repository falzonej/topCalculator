const operators = {
  add: (x, y) => {
  	let result = x + y;
    return result
  },
  subtract: (x, y) => {
    let result = x - y;
    return result;
  },
  divide: (x, y) => {
  	result = x / y;
    return result;
  },
  multiply: (x, y) => {
    result = x * y; 
    return result;
  }
}

const operate = (operators) => {
  result = operators;
  return result;
}